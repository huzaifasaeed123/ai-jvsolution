# ---- Backend (NestJS) environment ----
# Copy to .env and fill values. Never commit real secrets.

# App
NODE_ENV=development
PORT=4000
API_PREFIX=api

# Database (PostgreSQL) — used by Prisma
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/jvsolution?schema=public"

# Auth / JWT
JWT_ACCESS_SECRET=change-me-access
JWT_REFRESH_SECRET=change-me-refresh
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

# CORS — allowed web origins (comma-separated)
CORS_ORIGINS=http://localhost:3000

# File storage (S3-compatible) — Phase 1+
S3_ENDPOINT=
S3_REGION=
S3_BUCKET=
S3_ACCESS_KEY=
S3_SECRET_KEY=

# AI (Phase 4) — chatbot/avatar
ANTHROPIC_API_KEY=

# Payments (Phase 2+)
STRIPE_SECRET_KEY=
